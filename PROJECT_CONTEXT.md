# Project Context Pointer

The concise canonical product brief is [`docs/project-context.md`](docs/project-context.md).

Use the task-based routing table in [`AGENTS.md`](AGENTS.md) instead of loading every project document each session.
