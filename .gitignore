# secrets — never commit
.env
.env.*
!.env.example
*.key
*.seed

# deps & build
node_modules/
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# app
.next/
out/
dist/
web/data/*.sqlite
web/data/*.sqlite-shm
web/data/*.sqlite-wal

# evidence/ IS committed on purpose: real testnet identifiers are the demo fallback.
# Scripts must never write seeds or keys into it.

.DS_Store

# agent scratch output
.superpowers/
data/uk-law/snapshot.proposed.json
